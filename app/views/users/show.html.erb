<p style="color: green"><%= notice %></p>

<%= render @user %>

<div>
  <%= button_to "Edit your account", edit_user_path(@user) %>
  <!-- %= link_to "Back to users", users_path % -->
</div>

<div>
  <%= button_to "Delete your account?", user_path(@user), method: :delete, data: { confirm: 'Are you sure you want to delete this user?' } %>
</div>
