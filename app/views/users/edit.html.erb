<div class="container my-5">

  <div class="card bg-dark text-light p-4">
    <%= form_with model: @user, url: user_path(@user), method: :patch, local: true do |f| %>
      <div class="mb-3">
        <%= f.label :username, "Change Username:", class: "form-label" %>
        <%= f.text_field :username, class: "form-control" %>
      </div>

      <div class="mb-3">
        <%= f.label :email, "Change Email:", class: "form-label" %>
        <%= f.text_field :email, class: "form-control" %>
      </div>

      <div class="mb-3">
        <%= f.label :password, "Change Password:", class: "form-label" %>
        <%= f.password_field :password, class: "form-control", placeholder: "Enter new password" %>
      </div>

      <div class="d-flex justify-content-between">
        <%= f.submit "Save Changes:", class: "btn btn-success" %>
        <%= link_to "Discard Changes", edit_user_path(@user, discard: true), class: "btn btn-secondary" %>
      </div>

    <% end %>

    <div class="d-grid mt-3">
      <%= button_to "Delete Account", user_path(@user),
          method: :delete,
          onclick: "return confirm('Are you sure you want to delete your account? This action cannot be undone.');",
          class: "btn btn-danger" %>
    </div>
  </div>
</div>